pandas>=2.0
numpy>=1.24
openpyxl>=3.1
streamlit>=1.35
reportlab>=4.0
anthropic>=0.25
rapidfuzz>=3.0
requests>=2.28
Pillow>=10.0
pypdf>=4.0
playwright>=1.40

# ── Google Sheets/Drive (subir_prueba_sheets.py, publicar_drive.py) ─────────
gspread>=6.0
google-auth>=2.25
google-auth-oauthlib>=1.2
google-api-python-client>=2.100

# ── Solo Windows: fallback opcional de conversión Excel->PDF vía COM
# (skill_gt/scripts/generar.py). No es necesario si Excel no está instalado
# o el resto de la generación de PDF (reportlab) ya cubre el caso.
pywin32>=306; sys_platform == "win32"
